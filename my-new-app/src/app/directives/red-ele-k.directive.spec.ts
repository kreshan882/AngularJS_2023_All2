import { RedEleKDirective } from './red-ele-k.directive';

describe('RedEleKDirective', () => {
  it('should create an instance', () => {
    const directive = new RedEleKDirective();
    expect(directive).toBeTruthy();
  });
});
